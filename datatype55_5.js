["Bilbo","Gandalf","Nazgu1"].forEach((item,index,array) => {
    console.log(`${item} is at index ${index} in ${array}`)
});

(item) => {
    console.log(item)
}

function arry2(item){
    console.log(item)
}